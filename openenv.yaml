name: traffic-env
version: 1.0

endpoints:
  reset: /reset
  step: /step

environment:
  observation_space:
    type: discrete
    size: 81

  action_space:
    type: discrete
    size: 4

reward:
  type: custom

termination:
  condition: step_limit
  max_steps: 50